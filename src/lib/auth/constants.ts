export const SESSION_COOKIE = 'growthos-session';
export const ACTIVE_ROLE_COOKIE = 'growthos-active-role';

export const ROLE_HOME_PATHS: Record<string, string> = {
  employee: '/employee/home',
  manager: '/manager/home',
  hr: '/hr/home',
};

export const PUBLIC_PATHS = ['/login'];

export const AUTH_PATHS = ['/login'];
